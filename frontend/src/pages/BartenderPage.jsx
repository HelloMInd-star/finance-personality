import PlaceholderPage from '../components/Common/PlaceholderPage';

const BartenderPage = () => (
  <PlaceholderPage
    icon="🍸"
    title="调酒"
    description="P0 功能：6步调酒流程 + 故事生成 + 调酒师切换 —— 即将上线"
  />
);

export default BartenderPage;
